---
title: Google Sheets
description: Connect with your google account and fill form using google sheets data directly in iterative manner.
tags: [action, value, google-sheets, action-value]
toc: true
aliases:
  - '/docs/4.x/action-value/google-sheets/'
---

import { getDocsRelativePath } from '@libs/path'

Before using Google Sheets Value please do connect extension with [Google Sheets]([[docsref:/settings/google-sheets]]) and add [Google Sheet ID]([[docsref:/automation/settings#google-sheet-id]])

You can use [Google Sheets A1 Notation](https://developers.google.com/sheets/api/guides/concepts#:~:text=The%20Google%20Sheets%20API%20is,Update%20spreadsheet%20formatting) to fetch the cell value

<Example
  filePath={getDocsRelativePath('/src/content/html/input2.html')}
  variables={{
    element_finder: '//input',
    value: 'GoogleSheets::Sheet1!A1',
    element_finder2: '//input',
    value2: 'GoogleSheets::Sheet1!B1'
  }}
/>

## GoogleSheets::Sheet1!A1

Fetch the A1 cell value from Sheet1.

<Code code={`GoogleSheets::Sheet1!A1`} lang="javascript" />

## GoogleSheets::Sheet1!C

Fetch the C cell value from Sheet1.

<Code code={`GoogleSheets::Sheet1!C`} lang="javascript" />

## GoogleSheets::Sheet1!C&lt;loopRepeat&gt;

With loop repeat 0..N, fetch values from C1, C2, ... C(N+1).

<Code code={`GoogleSheets::Sheet1!C<loopRepeat> `} lang="javascript" />

`<batchRepeat>` still works as a legacy alias.

## GoogleSheets::Sheet1!C&lt;sessionCount&gt;

Maintains count in session; on refresh/reopen continues where it left off (same tab).

<Code code={`GoogleSheets::Sheet1!C<sessionCount>`} lang="javascript" />

<Callout name="subscribe-callout" />
